import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  texture: {
    flex: 1,
    width: '100%',
    height: '85%',
    resizeMode: 'contain',
    position: 'absolute',
    opacity: 0.2,
    padding: 'auto',
  },
});
